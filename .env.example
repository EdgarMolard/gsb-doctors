# Configuration Docker pour GSB-DOCTORS
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# Base de données
DB_NAME=gsbrapports
DB_USER=gsb_user
DB_PASSWORD=gsb_password
DB_ROOT_PASSWORD=gsb_root_2026
DB_PORT=3306

# API Backend
API_PORT=3000

# Frontend
FRONTEND_PORT=4200

# CORS (Cross-Origin Resource Sharing)
CORS_ORIGIN=http://localhost:4200

# JWT (JSON Web Token) - CLÉ SECRÈTE À CHANGER ABSOLUMENT EN PRODUCTION !
# Générez une clé aléatoire forte (minimum 32 caractères)
# Exemple de génération: openssl rand -base64 32
# Clé ci-dessous valable pour développement uniquement
JWT_SECRET_KEY=dev_secret_key_GSB2026_DO_NOT_USE_IN_PRODUCTION_xyz789
# Durée de validité du token en secondes (86400 = 24h, 3600 = 1h)
JWT_TOKEN_VALIDITY=86400

# PHP Configuration (pour développement)
PHP_DISPLAY_ERRORS=0
PHP_ERROR_REPORTING=E_ERROR

# Environnement
NODE_ENV=production
